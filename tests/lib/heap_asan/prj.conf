CONFIG_ZTEST=y

CONFIG_HEAP_ASAN=y
# Auto-track z_malloc_heap so malloc()/free() are monitored.
CONFIG_HEAP_ASAN_MALLOC=y
# Auto-track _system_heap so k_malloc()/k_free() are monitored.
CONFIG_HEAP_ASAN_SYSTEM=y

CONFIG_COMMON_LIBC_MALLOC=y
CONFIG_COMMON_LIBC_MALLOC_ARENA_SIZE=4096

# Give the kernel heap enough room for _system_heap tests.
CONFIG_HEAP_MEM_POOL_SIZE=4096

